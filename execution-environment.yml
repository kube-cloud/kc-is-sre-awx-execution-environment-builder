version: 3

# Configure Images
images:
  base_image:
    name: quay.io/ansible/awx-ee:24.6.1

# Dependencies
dependencies:
  python: requirements.txt
  galaxy: requirements.yml
  system: requirements-os.txt

# Additional Steps
additional_build_steps:
  append_base: |
    RUN $PYCMD install --upgrade pip setuptools